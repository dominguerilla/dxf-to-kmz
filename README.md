# .DXF Converter
A web application that can take one or more Drawing Exchange Format (.dxf) files and convert it into Keyhole Markup Language (.kml/.kmz) files.
The user uploads the .dxf files on a web page and when submitted, the application will convert it into .kml or .kmz files that the user can download.
